# HOMIGO — Architecture

Monorepo with three apps:

- `apps/web` — Next.js (App Router) frontend
- `apps/backend` — Bun + Elysia.js API
- `apps/mobile` — React Native app (Phase 5)

Backend uses Prisma + PostgreSQL and Redis. Detailed diagrams land in Phase 3.
