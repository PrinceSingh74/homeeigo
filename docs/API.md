# HOMIGO — API

Phase 1 endpoints (backend):

- `GET /health` — service health check
- `GET /api/v1/status` — app status / uptime
- `POST /api/v1/test` — echo test endpoint
- `GET /swagger` — interactive API docs

Full API reference is generated in Phase 3.
