// HOMIGO Prisma schema — Phase 1 placeholder
// Full models (User, Provider, Booking, Payment) land in Phase 3.

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}
