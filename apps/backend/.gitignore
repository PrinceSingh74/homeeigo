
# Local file uploads (rating photos, partner docs)
uploads/

# Database backups (scripts/backup-db.ts) — never commit dumps
backups/

# Env files (secrets) — keep only the committed template .env.example
.env
.env.*
!.env.example
!.env.staging.example
